# #1 — Первый автомат

- Source: [GitHub Issue #1](https://github.com/kshakirov/MZLS-engine/issues/1)
- State: OPEN
- Author: [kshakirov](https://github.com/kshakirov)
- Assignees: [kshakirov](https://github.com/kshakirov)
- Labels: none
- Created: `2026-08-06T14:23:37Z`
- Updated: `2026-08-27T12:52:20Z`
- Closed: —

## Milestone

### Milestone 1: "Core LL(1) HTTP Engine & BIO Template

Стабилизация базового ядра «MZLS-ENGINE». Архитектура блокирующего ввода-вывода (BIO) на Unix Domain Sockets за прокси Nginx, управление ресурсами через пулы виртуальных потоков (Project Loom) и детерминированный бесконструктный разбор стартовой строки HTTP-запроса.

Due date: —

## Description

Реализовать и стабилизировать детерминированный LL(1) конечный автомат разбора стартовой строки (Method, URI, Version) и управление жизненным циклом сокета без кучи аллокаций памяти.

## Todo Checklist

- [x] Настроить ручную конфигурацию `ThreadPoolExecutor` (вместо безграничной очереди) для организации Backpressure.
- [x] Реализовать очистку файловой системы от старого файла Unix-сокета (`.sock`) перед биндингом `ServerSocketChannel`.
- [x] Внедрить `Runtime.getRuntime().addShutdownHook` для Graceful Shutdown и автоудаления сокета при остановке сервера.
- [x] Внедрить промежуточное состояние автомата `STATUS.CHECK_NEXT_LINE` для изоляции запроса вообще без заголовков (`\r\n\r\n`).
- [ ] Исправить логический капкан со мгновенным выходом из середины цикла в кейсе `STATUS.FINISHED` (дать циклу честно дожевать последний `0x0A`).
- [ ] Заменить 4 независимых указателя на заголовки на один сквозной ленточный курсор `nextOffsetIdx++`.
- [ ] Выровнять арифметику каретки `versionEnd` в `STATUS.REQ_VERSION` для точного захвата границ `[HTTP/1.1]`.

## Context

- **Branch:** `dev`
- **Component:** `WirthHttpParser.java`
- **Target Metrics:** Стабильные 23k+ RPS без OOM под пиковой нагрузкой.

## Comments

### [kshakirov](https://github.com/kshakirov) — `2026-08-27T12:52:20Z`

Промежуточный статус:

- Я дотащил `WirthHttpParser` до контрольной точки: стартовая строка и заголовки разбираются, наружу летит таблица полуоткрытых offsets `[start, end)`. Ручной стенд `TestWirthHttpParser` работает.
- Я воткнул автомат в живой тракт `wrk → Nginx → Unix socket → сервак` и прогнал smoke: 225205 запросов за 10.01 с, 22495 req/s, без ошибок `wrk`. Зверь дышит.
- Я начал `HttpRequestParser`, но пока это только скелет. Сейчас я заглушаю им сервак и развожу два контрольных рубежа: отдельный тест для автомата и отдельный тест для оболочки.
- Я ещё не собрал один логический запрос из любого числа socket chunks и не протащил между ними состояние, курсор и offsets. Следом я зафиксирую контракт `FINISHED / INCOMPLETE / ERROR`, границу тела и остаток следующего запроса.
- Я отдельно вернусь к долгам сервака: полной записи ответа, закрытию канала, видимым ошибкам и расхождению старого чеклиста с virtual threads.

Тикет пока не закрываю: первый автомат стоит на контрольной точке, основная драка теперь в оболочке.
